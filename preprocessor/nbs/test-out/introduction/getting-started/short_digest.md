**SvelteKit setup**: `npx sv create myapp && cd myapp && npm install && npm run dev`

**Alternatives**: Vite (`npm create vite@latest` + select svelte), Rollup, Webpack plugins. Vite recommended.

**Tooling**: VS Code extension, editor integrations, `sv check` CLI.

**Support**: Discord, Stack Overflow.