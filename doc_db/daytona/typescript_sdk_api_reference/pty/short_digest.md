## PTY Session Types

**PtyCreateOptions**: Create PTY with `id`, optional `cols`, `rows`, `cwd`, `envs`

**PtyConnectOptions**: Connect to PTY with `onData()` callback for output

**PtyResult**: Exit status with `exitCode` and optional `error`