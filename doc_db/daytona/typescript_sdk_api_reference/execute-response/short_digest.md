## ExecuteResponse

Response from command execution with `exitCode`, `result` (output), and optional `artifacts`.

## ExecutionArtifacts

Artifacts from execution: optional `charts` array (matplotlib metadata) and `stdout` string.