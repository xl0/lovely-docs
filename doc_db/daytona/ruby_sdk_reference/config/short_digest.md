## Config

Configuration class for Daytona SDK with constructor accepting `api_key`, `jwt_token`, `api_url`, `organization_id`, `target`, and `_experimental` (all optional, defaulting to environment variables). All properties have getter/setter methods.