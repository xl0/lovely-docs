## Registry

Create your own component registry to distribute custom components and files to Svelte projects. Registry items must be valid JSON files conforming to the registry-item schema.

Clone the template:
```bash
npx degit huntabyte/shadcn-svelte/registry-template#next-tailwind-4
```