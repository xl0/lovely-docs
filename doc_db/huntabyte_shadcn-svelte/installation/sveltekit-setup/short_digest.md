## Setup

Create SvelteKit project with TailwindCSS, run `npx shadcn-svelte@latest init`, configure aliases and colors, then add components with `npx shadcn-svelte@latest add button -y -o` (use `-y` to skip prompts, `-o` to overwrite).

Import from `$lib/components/ui/component-name`.