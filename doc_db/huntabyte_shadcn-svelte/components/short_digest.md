60+ composable UI components installable via `npx shadcn-svelte@latest add <component> -y -o`. Built on Bits UI, Embla, LayerChart. Each has subcomponents (Root/Trigger/Content/etc), supports variants/sizes, integrates with forms via sveltekit-superforms. Examples: Accordion (single/multiple), Alert (default/destructive), Avatar (image+fallback), Badge (4 variants), Breadcrumb (with dropdowns/ellipsis), Button (6 variants, href support), Calendar (single/range, natural language), Card (flexible layout), Carousel (Embla-based), Chart (LayerChart), Checkbox/Radio/Switch (form integration), Combobox (Popover+Command), Command (menu), ContextMenu (right-click), DataTable (TanStack v8, pagination/sorting/filtering), DatePicker (Popover+Calendar), Dialog/Drawer (modal/slide-out), DropdownMenu (items/groups/checkboxes), Empty (state with media), Field (accessible form wrapper), Form (Zod+Superforms), HoverCard (preview on hover), Input/Textarea (with validation), InputGroup (icons/buttons/addons), InputOTP (pattern validation), Item (flex container), Kbd (keyboard display), Label, Menubar (desktop menu), NativeSelect (HTML select), NavigationMenu (links), Pagination (pages/ellipsis), Popover (rich content), Progress (bar), RangeCalendar (date range), Resizable (panels), ScrollArea (custom scroll), Select (dropdown), Separator (divider), Sheet (slide-out), Sidebar (collapsible with Provider), Skeleton (loading), Slider (range), Sonner (toast), Spinner (loading), Table (responsive), Tabs (tabbed), Toggle/ToggleGroup (two-state), Tooltip (hover popup), Typography (text utilities).