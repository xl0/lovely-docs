## Sonner

Toast component for Svelte. Install with `npx shadcn-svelte@latest add sonner -y -o`, add `<Toaster />` to root layout, then use `toast()` or `toast.success()` with optional description and action properties. Supports dark mode via system preferences or explicit theme prop.