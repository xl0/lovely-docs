60+ composable UI components (Accordion, Button, Form, Data Table, Sidebar, Calendar, Combobox, etc.) with subcomponents, variants, form integration. Install via CLI: `npx shadcn-svelte@latest add <component> -y -o`. Setup for SvelteKit/Vite/Astro/manual. CSS variable theming (OKLCH, light/dark modes). Dark mode via mode-watcher. Custom registries with JSON schema. Migrations: Svelte 4→5, Tailwind v3→v4. IDE extensions available.