**Svelte 4→5**: Update `components.json` aliases, add `tailwindcss-animate`, update `tailwind.config.js` with sidebar colors and animations, simplify `utils.ts`, upgrade bits-ui/svelte-sonner/@lucide/svelte/paneforge/vaul-svelte/mode-watcher, replace cmdk-sv/svelte-headless-table/lucide-svelte, run `npx shadcn-svelte@latest add <component> -y -o` per component.

**Tailwind v3→v4 + Svelte 5**: Replace PostCSS with Vite (`@tailwindcss/vite`), replace `tailwindcss-animate` with `tw-animate-css`, update `app.css` with `@import "tailwindcss"`, `@custom-variant dark`, HSL variables, and `@theme inline`, remove `tailwind.config.ts`, update dependencies, optionally update all components with `npx shadcn-svelte@latest add --all --overwrite`.