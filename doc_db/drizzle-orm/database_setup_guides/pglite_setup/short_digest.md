## PGlite Setup

Install `@electric-sql/pglite`, set `DATABASE_URL` env variable, connect Drizzle to PGlite, define tables, create `drizzle.config.ts` with PostgreSQL dialect, apply migrations, seed/query database, and run with tsx.