## SingleStore Setup with Drizzle ORM

Install mysql2, set DATABASE_URL environment variable, connect Drizzle ORM using `drizzle-orm/singlestore` package, define schema, create drizzle.config.ts with dialect 'singlestore', apply migrations, seed/query database, and run your TypeScript file.

Prerequisites: dotenv, tsx, mysql2