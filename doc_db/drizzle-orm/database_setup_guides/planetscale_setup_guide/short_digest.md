## PlanetScale Setup with Drizzle ORM

Install `@planetscale/database`, set environment variables (DATABASE_HOST, DATABASE_USERNAME, DATABASE_PASSWORD), connect Drizzle to PlanetScale using the HTTP driver, create tables, configure Drizzle with MySQL dialect, apply migrations, and query the database. Uses `database-js` for serverless HTTP connections; use MySQL guide for TCP connections.