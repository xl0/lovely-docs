## TiDB Setup with Drizzle

Install `@tidbcloud/serverless` driver, set DATABASE_URL environment variable, connect Drizzle to TiDB, create tables, configure Drizzle with mysql dialect, apply migrations, seed/query data, and run the application. Use this for HTTP connections; refer to MySQL guide for TCP connections.