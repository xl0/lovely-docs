## $app/environment

Runtime environment flags:
- `browser: boolean` — Running in browser
- `building: boolean` — During build/prerender step
- `dev: boolean` — Dev server running
- `version: string` — From `config.kit.version.name`