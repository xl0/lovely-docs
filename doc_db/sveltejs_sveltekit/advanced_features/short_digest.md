**Routing**: Rest `[...file]`, optional `[[param]]`, matchers `[type]`, encoding `[x+nn]`, groups `(name)`, reset `@ancestor`. **Hooks**: `handle` (request), `locals` (custom data), `handleFetch` (intercept), `handleError` (errors), `init` (startup), `reroute` (URL mapping), `transport` (serialization). **Errors**: Expected via `error()` render `+error.svelte`; unexpected expose generic message via `handleError`. **Links**: `data-sveltekit-preload-data/code` (eager/hover/tap/viewport), `reload`, `replacestate`, `keepfocus`, `noscroll`. **Service Workers**: Auto-register from `src/service-worker.js`, access `$service-worker` module (`build`, `files`, `version`), implement cache/network strategies. **Server-only**: `.server` suffix or `$lib/server/` directory prevents browser exposure. **Snapshots**: Export `snapshot` with `capture()`/`restore()` to preserve DOM state across navigation. **Shallow Routing**: `pushState`/`replaceState` for modals, `preloadData()` to load route data. **Observability**: OpenTelemetry tracing via `event.tracing` spans. **Packaging**: `@sveltejs/package` builds libraries; configure `exports`, `types`, `sideEffects` in package.json; auto-generate types; avoid `$app` modules; use `typesVersions` for non-root exports.