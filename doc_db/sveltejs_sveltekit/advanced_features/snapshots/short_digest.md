Export a `snapshot` object with `capture()` and `restore(value)` methods from `+page.svelte` or `+layout.svelte` to preserve DOM state across navigation. Data must be JSON-serializable and persists to `sessionStorage`.