Complete SvelteKit documentation: filesystem routing with `+page.svelte`/`+page.server.js`/`+layout.svelte`/`+server.js`, load functions returning data, form actions with `use:enhance`, page options (`prerender`, `ssr`, `csr`), state management via context/snapshots/search params, type-safe remote functions (query/form/command/prerender). Build: Vite + adapter (cloudflare/netlify/node/static/vercel/auto). Advanced: rest/optional params, matchers, route groups, layout reset, hooks (handle/locals/handleFetch/handleError/reroute/transport), error handling, link preloading, service workers, server-only modules, shallow routing, observability. Best practices: auth (sessions vs JWT), performance (code-splitting, preloading, images via enhanced-img, CDN), accessibility (announcements, focus, lang), SEO (SSR, titles, sitemaps). API: response helpers, RequestEvent, cookies, navigation, page store, hooks, adapters, remote functions, environment modules, config. Migrations: SvelteKit 2 breaking changes, Sapper migration.